apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rollouts-test
spec:
  template:
    spec:
      containers:
        - name: rollouts-test
          image: nginx:stable
          ports:
          - name: http
            containerPort: 8080
            protocol: TCP      

