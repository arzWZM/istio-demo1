apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rollouts-test
spec:
  template:
    spec:
      containers:
        - name: app
          image: argoproj/rollouts-demo:yellow
          ports:
          - name: http
            containerPort: 80
            protocol: TCP      
